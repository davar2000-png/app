# 🚀 راهنمای نصب و اجرا

## 📥 مرحله ۱: دانلود پروژه

### روش ۱: دانلود از GitHub
1. به آدرس زیر بروید:
   ```
   https://github.com/davar2000-png/store-appnew
   ```

2. روی دکمه سبز **"Code"** کلیک کنید

3. گزینه **"Download ZIP"** را انتخاب کنید

4. فایل ZIP را در کامپیوتر خود ذخیره کنید

### روش ۲: Clone با Git
```bash
git clone https://github.com/davar2000-png/store-appnew.git
cd store-appnew
```

---

## 📦 مرحله ۲: استخراج فایل‌ها

اگر فایل ZIP را دانلود کرده‌اید:

1. روی فایل ZIP راست‌کلیک کنید
2. گزینه **"Extract All"** یا **"Extract Here"** را انتخاب کنید
3. صبر کنید تا فایل‌ها استخراج شوند

---

## 🚀 مرحله ۳: اجرای برنامه

### روش ساده (پیشنهادی):

1. CMD را باز کنید
2. به پوشه پروژه بروید:
   ```bash
   cd C:\Users\TechnoKala\store-appnew
   ```
   (مسیر را با مسیر واقعی خود جایگزین کنید)

3. دستور زیر را اجرا کنید:
   ```bash
   start.bat
   ```

4. صبر کنید تا:
   - Node.js نصب شود (اگر نصب نیست)
   - پروژه build شود
   - سرور اجرا شود
   - مرورگر باز شود

5. برنامه در آدرس `http://localhost:8000` باز می‌شود

---

## 🔧 نصب Node.js (اگر نصب نیست)

1. به آدرس زیر بروید:
   ```
   https://nodejs.org/
   ```

2. نسخه **LTS** را دانلود کنید

3. فایل نصب را اجرا کنید

4. مراحل نصب را دنبال کنید

5. کامپیوتر را ریستارت کنید

---

## 📋 دستورات مفید

### نصب وابستگی‌ها (اولین بار):
```bash
npm install
```

### اجرای برنامه:
```bash
start.bat
```

### یا دستی:
```bash
npm run build
node server.js
```

---

## ❓ عیب‌یابی

### خطای "dist folder not found"
```bash
npm run build
```

### خطای "Node.js not found"
Node.js را نصب کنید: https://nodejs.org/

### خطای "Cannot find module"
```bash
npm install
```

### پورت 8000 اشغال است
فایل `server.js` را ویرایش کنید و پورت را تغییر دهید:
```javascript
const PORT = 8080; // یا هر پورت دیگر
```

---

## 🎯 خلاصه مراحل

```
1. دانلود پروژه از GitHub
2. استخراج فایل ZIP
3. باز کردن CMD
4. cd به پوشه پروژه
5. اجرای start.bat
6. باز شدن مرورگر
7. استفاده از برنامه
```

---

## 📞 پشتیبانی

اگر مشکلی دارید:
1. مطمئن شوید Node.js نصب است
2. مطمئن شوید در مسیر درست هستید
3. دستور `npm install` را اجرا کنید
4. دوباره `start.bat` را اجرا کنید

---

**موفق باشید! 🎉**
