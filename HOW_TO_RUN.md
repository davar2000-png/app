# 🚀 راهنمای اجرای برنامه

## ❌ مشکل شما

وقتی روی فایل `index.html` کلیک می‌کنید، برنامه باز نمی‌شود چون:
- فایل HTML به تنهایی کار نمی‌کند
- نیاز به فایل‌های JS و CSS در پوشه `assets` دارد
- مرورگر اجازه دسترسی مستقیم به فایل‌های محلی را نمی‌دهد

## ✅ راه حل‌ها

### روش ۱: استفاده از فایل start-server.bat (ساده‌ترین) ⭐

1. فایل `start-server.bat` را پیدا کنید
2. روی آن **دابل‌کلیک** کنید
3. مرورگر به صورت خودکار باز می‌شود
4. برنامه آماده استفاده است! ✅

**نکته:** این فایل نیاز به Python یا Node.js دارد.

---

### روش ۲: نصب Python (پیشنهادی)

#### نصب Python:

1. به https://www.python.org/downloads/ بروید
2. آخرین نسخه را دانلود کنید
3. نصب کنید (تیک "Add Python to PATH" را بزنید)
4. کامپیوتر را ریستارت کنید

#### اجرای برنامه:

```bash
# باز کردن Command Prompt در پوشه پروژه
# (Shift + Right Click در پوشه)

# رفتن به پوشه dist
cd dist

# اجرای سرور
python -m http.server 8000
```

سپس در مرورگر باز کنید:
```
http://localhost:8000
```

---

### روش ۳: نصب Node.js

#### نصب Node.js:

1. به https://nodejs.org/ بروید
2. نسخه LTS را دانلود کنید
3. نصب کنید

#### اجرای برنامه:

```bash
# نصب serve
npm install -g serve

# رفتن به پوشه dist
cd dist

# اجرای سرور
serve -l 8000
```

سپس در مرورگر باز کنید:
```
http://localhost:8000
```

---

### روش ۴: استفاده از VS Code (حرفه‌ای)

1. **VS Code** را نصب کنید: https://code.visualstudio.com/
2. افزونه **Live Server** را نصب کنید
3. پوشه پروژه را در VS Code باز کنید
4. روی `dist/index.html` راست‌کلیک کنید
5. **Open with Live Server** را بزنید
6. برنامه در مرورگر باز می‌شود ✅

---

### روش ۵: استفاده از مرورگر با Flag (موقت)

**هشدار:** این روش امن نیست و فقط برای تست استفاده کنید.

#### برای Chrome:

1. Chrome را ببندید
2. Command Prompt را باز کنید
3. این دستور را اجرا کنید:

**ویندوز:**
```cmd
"C:\Program Files\Google\Chrome\Application\chrome.exe" --allow-file-access-from-files --user-data-dir="C:\ChromeDevSession"
```

**مک:**
```bash
/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome --allow-file-access-from-files --user-data-dir="/tmp/ChromeDevSession"
```

4. سپس فایل `dist/index.html` را در این Chrome باز کنید

---

## 📋 خلاصه سریع

| روش | سختی | سرعت | پیشنهاد |
|------|------|------|---------|
| start-server.bat | آسان | سریع | ⭐⭐⭐⭐⭐ |
| Python | متوسط | سریع | ⭐⭐⭐⭐ |
| Node.js | متوسط | سریع | ⭐⭐⭐⭐ |
| VS Code | آسان | سریع | ⭐⭐⭐⭐ |
| Chrome Flag | سخت | کند | ⭐⭐ |

---

## 🎯 بهترین روش برای شما

### اگر Python یا Node.js دارید:
✅ فایل `start-server.bat` را اجرا کنید

### اگر ندارید:
1. Python را نصب کنید (ساده‌ترین)
2. سپس `start-server.bat` را اجرا کنید

### اگر نمی‌خواهید چیزی نصب کنید:
✅ از VS Code با افزونه Live Server استفاده کنید

---

## 💡 نکات مهم

### بعد از اجرا:

1. مرورگر به صورت خودکار باز می‌شود
2. آدرس `http://localhost:8000` را می‌بینید
3. برنامه آماده استفاده است
4. داده‌ها در مرورگر ذخیره می‌شوند

### برای توقف سرور:

- پنجره Command Prompt را ببندید
- یا `Ctrl+C` را بزنید

### برای اجرای دوباره:

- فقط `start-server.bat` را اجرا کنید
- یا دستورات بالا را تکرار کنید

---

## ❓ مشکلات رایج

### "Python پیدا نشد"

**راه حل:**
1. Python را نصب کنید
2. تیک "Add Python to PATH" را بزنید
3. کامپیوتر را ریستارت کنید
4. دوباره تلاش کنید

### "Port 8000 در حال استفاده است"

**راه حل:**
```bash
# استفاده از پورت دیگر
python -m http.server 8080
```

سپس در مرورگر:
```
http://localhost:8080
```

### "فایل‌ها باز نمی‌شوند"

**راه حل:**
1. مطمئن شوید در پوشه `dist` هستید
2. مطمئن شوید سرور در حال اجراست
3. مرورگر را رفرش کنید (F5)

---

## 📞 پشتیبانی

اگر مشکل حل نشد:

1. فایل `GITHUB_GUIDE.md` را مطالعه کنید
2. Issue در GitHub باز کنید
3. اسکرین‌شات خطا را ارسال کنید

---

**موفق باشید! 🚀**
