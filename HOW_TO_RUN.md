# 🚀 راهنمای اجرای برنامه

## 📁 فایل‌های اجرا

### ۱. **start.bat** (Python)
- **نیاز به:** Python نصب شده
- **دستور:** `start.bat`
- **آدرس:** http://localhost:8000

### ۲. **start-node.bat** (Node.js)
- **نیاز به:** Node.js نصب شده
- **دستور:** `start-node.bat`
- **آدرس:** http://localhost:8000

---

## 🐍 نصب Python

1. به https://www.python.org/downloads/ بروید
2. آخرین نسخه را دانلود کنید
3. **مهم:** تیک "Add Python to PATH" را بزنید
4. نصب کنید
5. `start.bat` را اجرا کنید

---

## 🟢 نصب Node.js

1. به https://nodejs.org/ بروید
2. نسخه LTS را دانلود کنید
3. نصب کنید
4. `start-node.bat` را اجرا کنید

---

## 🖥️ اجرای مستقیم (بدون سرور)

اگر نمی‌خواهید سرور اجرا کنید:

1. پوشه `dist` را باز کنید
2. فایل `index.html` را در مرورگر باز کنید
3. برنامه اجرا می‌شود

**توجه:** این روش ممکن است برای برخی قابلیت‌ها کار نکند.

---

## ❓ عیب‌یابی

### خطای "Python not found"
- Python نصب نیست
- یا PATH تنظیم نشده
- **راه حل:** Python را دوباره نصب کنید با تیک "Add to PATH"

### خطای "Node.js not found"
- Node.js نصب نیست
- **راه حل:** Node.js را نصب کنید

### خطای "Port 8000 in use"
- پورت ۸۰۰۰ اشغال است
- **راه حل:** برنامه دیگری که از این پورت استفاده می‌کند را ببندید

### برنامه باز نمی‌شود
- فایل `dist/index.html` وجود ندارد
- **راه حل:** ابتدا `npm run build` را اجرا کنید

---

## 📝 دستورات مفید

### ساخت برنامه
```bash
npm run build
```

### اجرای سرور توسعه
```bash
npm run dev
```

### اجرای سرور تولید
```bash
cd dist
python -m http.server 8000
```

---

## 🎯 پیشنهاد

اگر Python و Node.js هر دو نصب هستند:
- **start.bat** را استفاده کنید (سریع‌تر)

اگر فقط Node.js نصب است:
- **start-node.bat** را استفاده کنید

اگر هیچکدام نصب نیست:
- Python را نصب کنید (ساده‌تر)

---

**موفق باشید! 🚀**
